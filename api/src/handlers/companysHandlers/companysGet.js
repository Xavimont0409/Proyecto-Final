const errorUser = require('../../helpers/errors')

function companysHandlerGet(req, res){
    try {
        
    } catch (error) {
        errorUser(error, res);
        
    }
}

module.export={
    companysHandlerGet
}